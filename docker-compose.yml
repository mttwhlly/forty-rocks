version: '3.8'

services:
  nyc-birthday-app:
    build: .
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    labels:
      - "coolify.managed=true"
